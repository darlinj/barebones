type Query {
  getData(id: ID!): Data
  getLambdaData: LambdaData
  getPublicData(id: ID!): PublicData @aws_api_key
}

type Data {
  id: ID!
  name: String!
}

type PublicData @aws_api_key {
  id: ID!
  name: String!
}

type LambdaData {
  message: String!
}
